{
  "name": "reality-editor-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Two endpoints. Not in the op path, not on the critical path.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "codegen": "../contracts/codegen.sh"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.125.0",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1",
    "zod": "^4.6.3",
    "@reality/contracts": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "pino-pretty": "^13.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
